# URL of the recruiter-dashboard Vercel API (no trailing slash)
# This gets baked into the plugin bundle at build time
FLEX_APP_CANDIDATE_API_URL=https://your-recruiter-dashboard.vercel.app

# Public URL where the plugin's static assets are served (for logo)
# Leave blank during local dev — the CLI sets this automatically on deploy
FLEX_APP_PUBLIC_URL=
